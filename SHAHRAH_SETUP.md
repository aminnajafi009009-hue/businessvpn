# اتصال پنل شاهراه به ربات — راهنما

## ۱) تنظیم اولیه (فقط یک‌بار)
در فایل `.env` روی سرور (Render و…) این دو خط را اضافه کن:

```
SHAHRAH_API_KEY=توکن_واقعی_خودت_را_اینجا_بگذار
SHAHRAH_BASE_URL=http://shahrah.top/api/vaas/reseller
```

⚠️ **توکن API را که در چت فرستادی جای امنی نگه ندار و بهتره از پنل شاهراه یک‌بار Regenerate/rotate کنی**، چون هر جایی که تایپ بشه (حتی داخل یک چت با هوش مصنوعی) دیگه واقعاً «فقط مال خودت» حساب نمی‌شه. توکن جدید را فقط در `.env` سمت سرور بگذار؛ در کد هاردکد نشده و به مینی‌اپ/کلاینت هم فرستاده نمی‌شه.

اگر `SHAHRAH_API_KEY` خالی بمونه، کل این بخش خودکار غیرفعال/مخفی می‌مونه و ربات دقیقاً مثل قبل (۱۰۰٪ دستی) کار می‌کنه.

`requirements.txt` یک خط جدید داره: `qrcode[pil]` (برای ساخت خودکار عکس کیوآرکد از لینک ساب). قبل از دیپلوی حتماً `pip install -r requirements.txt` رو دوباره اجرا کن.

## ۲) چیزی که تغییر نکرد
هیچ گزینه یا مسیر قبلی ربات حذف/تغییر نشده. دکمه‌ی «🚀 ارسال کانفیگ VIP (QR) — دستی» و «📤 شروع ارسال کانفیگ — دستی» دقیقاً همون رفتار قبلی رو دارن.

## ۳) چیزی که اضافه شد
- منوی ادمین → **🔗 اتصال پنل شاهراه**: تست اتصال، مشاهده‌ی بسته‌ها (`/plans`)، مشاهده‌ی ترافیک (`/traffic`)، و نگاشت دسته‌بندی‌های **VIP** (فقط VIP) به یک بسته‌ی مشخص شاهراه.
- بعد از تأیید رسید یک خرید VIP: اگر دسته‌بندی اون پلن قبلاً نگاشت شده باشه، یک دکمه‌ی اضافه («📤 ارسال خودکار از پنل شاهراه») کنار دکمه‌ی دستی نشون داده می‌شه. **انتخاب کدوم روش همیشه با خودته.**
- ⚠️ **Gaming کاملاً و عمداً جداست و به این بخش وصل نشده و نمی‌شه.** هیچ نگاشت، هیچ دکمه‌ی خودکار و هیچ تماس APIای برای گیمینگ وجود نداره؛ ارسال کانفیگ گیمینگ همیشه ۱۰۰٪ دستی می‌مونه، دقیقاً مثل قبل.
- بعد از تأیید رسید سفارش «بساز سرویس خودت»: دکمه‌ی «🧩 ساخت خودکار از پنل شاهراه» اضافه شده — لیست زنده‌ی بسته‌های شاهراه رو نشون می‌ده تا نزدیک‌ترین بسته رو انتخاب کنی.
- در صفحه‌ی جزئیات هر سرویسی که از طریق شاهراه ساخته شده: دکمه‌های «🔁 تمدید از پنل»، «⏸ غیرفعال‌کردن در پنل»، «▶️ فعال‌کردن در پنل».

## ۴) جواب سوال‌هات درباره‌ی ترافیک اقتصادی/CDN
تعیین اینکه هر **بسته** (planSlug) از کدوم استخر ترافیک (اقتصادی تانل یا CDN اروان) تغذیه بشه، **در خود پنل شاهراه** و در سطح تعریف بسته مشخص می‌شه — نه چیزی که از سمت ربات قابل تنظیم باشه، چون API شاهراه چنین پارامتری برای انتخاب ترافیک در `create`/`renew` ندارد.

ربات فقط این کار رو می‌کنه: هر **دسته‌بندی** ما (مثلاً «🚀 پرسرعت و کاربر نامحدود» یا یک دسته‌ی جدید مثل «کانفیگ نت ملی») رو به یک `planSlug` مشخص وصل می‌کنه (بخش «🗂 نگاشت دسته‌بندی‌ها»). پس روش عملی درست همینه:
1. توی پنل شاهراه دو (یا چند) بسته بساز: یکی که از «اقتصادی تانل» تغذیه می‌شه، یکی که از «CDN اروان».
2. توی ربات، دسته‌بندی «پرسرعت» رو به بسته‌ی اقتصادی نگاشت کن، و دسته‌بندی «کانفیگ نت ملی» (یا هر اسمی که ساختی) رو به بسته‌ی CDN.
از این به بعد وقتی مشتری از هرکدوم از این دو دسته بخره و بزنی «ارسال خودکار»، دقیقاً از همون ترافیکی که خودت مشخص کردی سرویس ساخته می‌شه.

## ۵) محدودیت‌های فعلی که خوبه بدونی
- مستندات شاهراه ساختار دقیق پاسخ `create`/`renew` رو نشون نمی‌ده؛ کد به‌صورت هوشمند (چند کلید رایج مثل `subscriptionUrl`/`link`/`slug`) لینک و شناسه‌ی سرویس رو پیدا می‌کنه و پاسخ خام رو هم همیشه برات نمایش می‌ده. اگر یک بار تشخیص خودکار جواب نداد، ازت می‌خواد لینک رو یک‌بار دستی پیست کنی (بقیه‌ی مراحل خودکار می‌مونه)؛ اگر لازم شد، کافیه کلیدهای واقعی رو بهم بگی تا `shahrah.py` رو دقیق‌تر کنم.
- «بساز سرویس خودت» چون حجم/مدت دلخواه کاربره و شاهراه بسته‌های ثابت داره، به‌جای انتخاب خودکارِ حجمِ دقیق، لیست زنده‌ی بسته‌ها رو نشونت می‌ده تا خودت نزدیک‌ترین رو انتخاب کنی (امن‌تر از حدس زدن).
