# مدیریت پنل‌های VPN (شاهراه / مرزبان / پاسارگارد) — راهنما

> این فایل جای `SHAHRAH_SETUP.md` قدیمی را می‌گیرد (که فقط مخصوص یک پنل شاهراه ثابت از `.env` بود). حالا هر سه نوع پنل هم‌زمان، و هرکدام با چند نمونه‌ی (اکانت) مستقل، از داخل خودِ ربات مدیریت می‌شوند — نیازی به تغییر `.env` یا ری‌استارت سرور برای افزودن/تغییر یک پنل نیست.

## ۱) چه چیزی عوض شد؟

- قبلاً فقط یک پنل شاهراه با کلید ثابت در `.env` پشتیبانی می‌شد.
- الان:
  - **سه نوع پنل** پشتیبانی می‌شود: 🛣 شاهراه، 🛡 مرزبان (Marzban)، 🦅 پاسارگارد.
  - از هر نوع می‌توانید **چند نمونه/اکانت جدا** اضافه کنید (مثلاً دو اکانت مرزبان روی دو سرور مختلف) و همه هم‌زمان فعال باشند.
  - هر پنل یک **دکمه‌ی مدیریت جداگانه** در پنل ادمین دارد (تست اتصال، فعال/غیرفعال، ویرایش، حذف، نگاشت).
  - بخش «نگاشت» (اینکه هر پلن/دسته‌بندی/بساز-سرویس-خودت/تست‌رایگان از کدام پنل و کدام بسته‌ی همان پنل ساخته شود) داخل صفحه‌ی مدیریت **همان پنل** قرار دارد — یعنی برای هر پنل جدا نگاشت می‌کنید، نه یک نگاشت سراسری.
  - داده‌ی همه‌ی پنل‌ها در دیتابیس ذخیره می‌شود (جدول `vpn_panels`)، نه در `.env`؛ افزودن/ویرایش/حذف کاملاً از داخل ربات و بدون دیپلوی مجدد انجام می‌شود.
  - پنل شاهراه‌ی قدیمی (از `.env`) در اولین اجرا پس از آپدیت، به‌صورت خودکار به یک ردیف در همین جدول migrate می‌شود؛ چیزی از دست نمی‌رود.

## ۲) از کجا شروع کنم؟

منوی ادمین → **🖥 مدیریت پنل‌های VPN**

از این‌جا:
1. یکی از سه نوع پنل را انتخاب می‌کنید (شاهراه / مرزبان / پاسارگارد).
2. فهرست نمونه‌های موجود از همان نوع را می‌بینید (اگر هنوز چیزی اضافه نکرده باشید، خالی است) + دکمه‌ی «➕ افزودن پنل جدید».
3. با زدن «➕ افزودن پنل جدید» به‌ترتیب از شما می‌پرسد:
   - **نام دلخواه** (فقط برای تشخیص خودتان، مثلاً «مرزبان سرور آلمان»)
   - **آدرس پایه (Base URL)** پنل
   - برای **شاهراه**: کلید API (API Key)
   - برای **مرزبان/پاسارگارد**: نام کاربری و رمز عبور ادمین پنل
4. بعد از ساخت، وارد صفحه‌ی جزئیات همان پنل می‌شوید که شامل:
   - 🔌 تست اتصال
   - ✏️ ویرایش (نام / آدرس / کلید یا نام‌کاربری‌رمزعبور)
   - ⏸/▶️ فعال یا غیرفعال کردن این نمونه (پنل غیرفعال در نگاشت‌های جدید و ارسال خودکار دیده نمی‌شود، ولی سرویس‌های قبلی‌اش دست‌نخورده می‌مانند)
   - 🗑 حذف (با تأیید)
   - 🗂 مدیریت نگاشت‌ها (بخش بعدی)

## ۳) نگاشت — یعنی «کدام پلن از کدام پنل ساخته شود»

داخل صفحه‌ی جزئیات هر پنل → **🗂 نگاشت‌های این پنل**، سه بخش وجود دارد:

- **🚀 دسته‌بندی‌های VIP**: انتخاب می‌کنید کدام دسته‌بندی VIP، با کدام بسته/قالب سرویس همین پنل ساخته شود.
- **🧩 بساز سرویس خودت**: بسته/قالب پیش‌فرض همین پنل برای سفارش‌های «بساز سرویس خودت».
- **🎁 تست رایگان**: بسته/قالب همین پنل برای پلن تست رایگان.

⚠️ **گیمینگ عمداً به هیچ پنلی وصل نمی‌شود** — ارسال کانفیگ گیمینگ همیشه ۱۰۰٪ دستی می‌ماند، دقیقاً مثل قبل.

چون نگاشت‌ها **در سطح هر پنل جدا ذخیره می‌شوند**، اگر یک دسته‌بندی VIP را هم به پنل A و هم به پنل B نگاشت کنید، آخرین نگاشتی که ذخیره کرده‌اید همان دسته‌بندی را به خودش وصل می‌کند (هر دسته‌بندی/بساز-سرویس-خودت/تست‌رایگان در کل فقط به **یک** پنل و یک بسته وصل است — همانی که آخرین بار ذخیره شده). با همین روش می‌توانید هر بسته را از هرکدام از پنل‌های فعال‌تان تغذیه کنید و هرجا خواستید نگاشت را عوض کنید.

## ۴) ارسال خودکار سرویس بعد از خرید

- **خرید VIP / تست رایگان**: اگر دسته‌بندی خریداری‌شده نگاشت شده باشد، همان لحظه (بدون نیاز به کار دستی ادمین) سرویس از پنل نگاشت‌شده ساخته و برای کاربر ارسال می‌شود؛ اگر نگاشت نباشد یا ساخت ناموفق باشد، دقیقاً مثل قبل به ادمین اطلاع داده می‌شود تا دستی/از پنل انتخابی ارسال کند (دکمه‌ی «📤 ارسال از پنل» زیر پیام سفارش).
- **بساز سرویس خودت**: اگر نگاشت پیش‌فرض ست شده باشد، خودکار ساخته می‌شود؛ در غیر این صورت، ادمین با دکمه‌ی «🧩 ساخت خودکار از پنل» می‌تواند یکی از پنل‌های فعال و سپس یک بسته از کاتالوگ آن پنل را انتخاب کند.
- در هر دو حالت، اگر لینک ساب به‌صورت خودکار از پاسخ پنل تشخیص داده نشود، از ادمین خواسته می‌شود یک‌بار دستی لینک را پیست کند؛ بقیه‌ی مراحل (ساخت QR، ارسال به کاربر، ذخیره در دیتابیس) خودکار می‌ماند.

## ۵) مدیریت سرویس‌های ساخته‌شده

در صفحه‌ی جزئیات هر سرویسی که از طریق یکی از این پنل‌ها ساخته شده باشد (چه VIP، چه بساز-سرویس-خودت):
- 🔁 تمدید از پنل (بسته‌ی جدید از همان پنل انتخاب می‌کنید)
- ⏸ غیرفعال‌کردن در پنل
- ▶️ فعال‌کردن در پنل

هرکدام از این‌ها همان پنل و همان `service_id` ذخیره‌شده‌ی آن سرویس را صدا می‌زنند؛ یعنی حتی اگر سرویس از پنل شماره‌ی ۲ (مثلاً مرزبانِ سرور آلمان) ساخته شده باشد، تمدید/غیرفعال/فعال هم دقیقاً روی همان پنل انجام می‌شود.

## ۶) محدودیت‌های فعلی

- «بساز سرویس خودت» چون حجم/مدت دلخواه کاربر است و پنل‌ها معمولاً بسته/قالب‌های ثابت دارند، به‌جای انتخاب خودکار حجم دقیق، کاتالوگ زنده‌ی همان پنل نشان داده می‌شود تا ادمین نزدیک‌ترین گزینه را انتخاب کند.
- گیمینگ کاملاً و همیشه دستی می‌ماند و هیچ اتصالی به این پنل‌ها ندارد.
- برای مرزبان/پاسارگارد، تشخیص خودکار لینک ساب از روی پاسخ create/renew به‌صورت هوشمند و با چند کلید رایج انجام می‌شود؛ اگر تشخیص شکست بخورد، مسیر دستی (پیست یک‌بار لینک) جایگزین می‌شود.
